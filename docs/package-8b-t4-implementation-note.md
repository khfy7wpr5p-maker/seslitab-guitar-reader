# Package 8B-T4 Implementation Note

This branch is intentionally bounded to a research-admission decision layer.

- It records that MUSCIMA++/CVC-MUSCIMA evidence is non-commercial research-only.
- It records Audiveris' glyph-classifier technical input as global `samples.zip` glyph+shape samples.
- It keeps Package 8B-T1's stricter `.omr` requirement unchanged.
- It adds an exact, separate T4 per-sample research approval binding.
- Without complete T4 approvals, current mapped evidence remains blocked.
- Complete T4 approvals can authorize only the later construction of an isolated research `samples.zip` artifact.
- It does not execute training, produce or replace a classifier model, publish MUSCIMA images, or change production OMR/deployment wiring.
